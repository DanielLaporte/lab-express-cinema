<div class="container">
  <h1>Movies</h1>
  <ul class="movie-list">
    {{#each movies}}
    <li>
      <img src="{{image}}" alt="{{title}} Poster">
      <h2>{{title}}</h2>
      <a class="see-more-link" href="/movies/{{_id}}">See more</a>
    </li>
    {{/each}}
  </ul>
</div>